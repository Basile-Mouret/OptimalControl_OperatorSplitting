In this project, we implemented the operator-splitting method of O'Donoghue, Stathopoulos, and Boyd @odonoghue2013 in Julia for finite-horizon optimal control with linear dynamics. On the benchmark examples, the Julia solver matches the reference C implementation in iteration counts, while runtime differences depend on the relative costs of the linear-system and proximal steps.

Several extensions would be natural. We could study the sensitivity to the hyperparameters ($rho$, $alpha$, and the stopping tolerances), broaden the comparison with other optimization methods, and test the solver on additional examples. A more complete theoretical study, including convergence-rate results, would also strengthen the analysis.
